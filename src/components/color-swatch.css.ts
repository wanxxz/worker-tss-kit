import { style } from '@vanilla-extract/css'

export const colorSwatchClassName = style({
  width: '32px',
  height: '32px',
  borderRadius: '4px',
  boxShadow: 'inset 0 0 0 1px rgba(0, 0, 0, 0.1)'
})
